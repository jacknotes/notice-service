# Local dev data (MariaDB, Go/npm caches) — large, must not ship
.dev

# VCS / tooling
.git
.superpowers
docs

# Node
**/node_modules
web/dist

# Env / logs
.env
.env.*
*.log

# 密钥与本地配置：绝不能进镜像（含构建上下文）
.notice-encrypt.key
*.key
config.yml
